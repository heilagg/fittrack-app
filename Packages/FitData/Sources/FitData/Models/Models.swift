//  SwiftData-модели. Зеркалят схему Postgres с двумя отличиями:
//
//  1. Нет user_id. Локальная база однопользовательская; user_id проставляет
//     слой синхронизации в момент выгрузки. Именно это делает первый запуск
//     в офлайне возможным — SPEC §4.1 требует анонимного входа сразу,
//     SPEC §4.3 требует работы без сети, и без этого приёма они конфликтуют.
//
//  2. Служебные поля: syncState (.synced/.pendingUpload/.conflicted),
//     updatedAt (LWW), deletedAt (soft delete; физическое удаление только
//     после подтверждённой выгрузки).
//
//  Массивы numeric[] (гантели, блины) → [Double].
