//  Progression — двойная прогрессия с RIR-модуляцией (SPEC §9).
//
//  Два слоя реакции:
//    1. nextSet(...)     — мгновенно, внутри сессии (SPEC §9.3)
//    2. betweenSessions  — по 2–3 последним сессиям (SPEC §9.4, §9.5, §9.7)
//
//  Центральная функция модуля — rebuildStates(from:): состояние есть свёртка
//  над журналом подходов. Из неё же следует разрешение конфликтов
//  синхронизации (SPEC §4.3) и инвалидация кэша при смене алгоритма.
//
//  Демпфирование baseline (SPEC §9.6) живёт только в BaselineUpdater
//  и нигде больше.
