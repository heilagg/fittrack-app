//  Readiness — сводный множитель готовности (SPEC §10).
//
//  readiness = clamp(1.0 + phaseAdj×confidence + checkinAdj + overrideAdj,
//                    0.75, 1.10)
//
//  Оверрайд заменяет фазовую поправку целиком, а не суммируется с ней,
//  и живёт один день (SPEC §11.4).
//
//  Граница ответственности: readiness влияет на вес и объём; утомление
//  и флаги боли — на выбор упражнений и целевой RIR. Пересечения нет
//  по построению, поэтому push не снимает защиту утомления.
