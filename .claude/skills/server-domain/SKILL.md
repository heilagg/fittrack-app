---
name: server-domain
description: Правила работы с веб-сервером FitTrack — пакет Server (SPEC §20): Vapor-обёртка над FitCore, пул postgres-nio, проверка JWT по JWKS, слой Mapping (схема §3.1 → типы FitCore), эндпоинты /v1, дерево решений §20.9, тесты §20.15 20a–20h. Используй эту скилл всегда, когда задача касается Server — включая «реализуй Server», «сделай Vapor-обёртку», «начни этап В0», «подними каркас сервера», «сделай эндпоинт /v1/…», «собери SessionInput на сервере», «закрой тесты 20a–20h» — и когда правка соседнего модуля (FitCore, FitContent, схема Supabase, Web) задевает то, что сервер читает, собирает или отдаёт. Действует вместе с implement-feature и переопределяет её §7 для пунктов списка §2, а также §1, §2, §5 и чек-лист — см. §4.
---

# Server Domain (FitTrack)

`Server` отличается от всех уже реализованных модулей тремя вещами, и каждая
меняет процедуру:

1. **Это первый модуль, который не является чистой логикой.** `implement-feature`
   §2 начинается словами «FitCore — zero-dependency» и описывает граф внутри
   одного пакета без внешних зависимостей. Server — первый пакет с внешними
   зависимостями (Vapor, `postgres-nio`, JWKS), первый, который собирается под
   Linux, и первый, чьи обязательные тесты требуют живого Postgres с
   применёнными миграциями и RLS. Ни одного из этих понятий в `implement-feature`
   нет.
2. **У Server нет doc-комментария модуля, а это единственный источник
   архитектуры по `implement-feature` §1.** Все шесть модулей FitCore получили
   свой на этапе скелета («Add project skeleton»), и процедура прямо велит
   начинать с его чтения. `Server/` в скелет не входил и не существует вовсе:
   архитектуру придётся не прочитать, а написать — и написать её нужно из §20, а
   не из головы.
3. **§20 писался по памяти о контрактах FitCore, а не сверкой с сигнатурами.**
   Отсюда список §2 не только длиннее, чем у планировщика, но и другого рода:
   часть пунктов — не «в SPEC не описано», а «в SPEC описано иначе, чем в коде»
   (пп. 12, 18, 20, 21, 23). Такой пункт опаснее пробела: пробел виден на первой
   же попытке скомпилировать, расхождение — нет, а два из них (12 и 20)
   обязательные тесты §20.15 по построению не ловят.

## 1. Порядок инвертирован для пунктов §2: SPEC правится ДО кода

`implement-feature` §7 требует не редактировать SPEC.md, а фиксировать
несостыковки в теле коммита и делать разумный выбор в коде. **Для пунктов
списка §2 ниже это правило не действует** — так же, как для §10/§11 его
отменяет [cycle-phase-domain](../cycle-phase-domain/SKILL.md) §1, а для своих
списков — [planner-domain](../planner-domain/SKILL.md) §1 и
[fitdata-domain](../fitdata-domain/SKILL.md) §1.

Причина здесь третья, отличная и от планировщика, и от FitData. Там молчаливый
выбор определял качество подбора и то, чьи данные исчезнут; здесь он определяет
**публичный контракт с двумя клиентами сразу**. §20.3 прямо говорит, зачем `/v1`
в пути с первого дня: «у API будет второй клиент (iOS), и он будет обновляться
не синхронно с вебом». Форма ответа, выбранная в Swift-файле, станет тем, под
что напишется фронтенд, а потом iOS, и менять её придётся уже с двумя
развёрнутыми клиентами. Плюс §20.8 задал прецедент прямо в тексте: `userSeed`
объявлен частью контракта, который «нельзя поменять никогда». Такие решения
живут в SPEC, а не в комментарии к роуту.

Процедура — та же, что в planner-domain §1 и fitdata-domain §1:

1. Остановиться, не выбирать вариант в коде.
2. Показать пользователю простым текстом в чате — что сказано текстом SPEC, что
   из этого следует, какие есть варианты и чем различаются их последствия для
   пользовательницы и для будущего iOS-клиента, а не только для кода. Без
   диалога и без заранее выбранного варианта: решение принимается до применения,
   а не подтверждается после.
3. Получить решение, внести в SPEC отдельным коммитом. Решение, задевающее схему
   Postgres, — по правилу §3: раньше миграций.
4. Только потом писать код.

Для остальных несостыковок в Server `implement-feature` §7 действует как есть.

## 2. Что должно быть решено в SPEC §20 до `/implement-feature Server`

Это список вопросов, а не ответов: варианты выбираются по процедуре §1 вместе
с пользователем. **Закрыто семь пунктов** — 7, 8, 9, 12, 13, 15, 21 (ветка
`spec/server-schema-decisions`, коммиты ниже), плюс §19.2 п.8; остальные
открыты. `implement-feature` для
Server не начинается, пока все пункты не закрыты в SPEC. Основной дом решения —
§20; если пункт задевает другой раздел (§3.1, §7.2, §9.1, §9.3, §9.8, §13.4,
§18, §19.2), тот правится в том же коммите. Закрытый пункт отмечается здесь со
ссылкой на коммит SPEC.

Пункты пяти родов:

- **1–6** — публичный контракт наружу, который §20.3 объявил обязательным и не
  задал;
- **7–11** — входы FitCore и операции, которым не нашлось места ни в §3.1, ни в
  таблицах §20.3;
- **12–21** — расхождения между текстом §20 и фактическими сигнатурами FitCore,
  причём 12 и 20 не ловятся обязательными тестами §20.15;
- **22** — обязанности `Mapping/`, задокументированные в FitCore и не названные
  в §20 вовсе;
- **23** — не решение, а правка текста: три места, где §20 описывает
  существующий код неверно.

### Контракт наружу

- [ ] **1. Форма ответов эндпоинтов (§20.3).** Ни одна строка обеих таблиц не
      задаёт схему тела. «Отдаёт: готовность (§10), фаза и уверенность (§11.3),
      тип дня, причины» — перечень содержимого, а не контракт. Решается вместе с
      п. 4: без DTO-слоя форму нечем выразить.
- [ ] **2. Закрытый словарь кодов ошибок (§20.3).** Объявлен обязательным
      («`code` из закрытого словаря») и нигде не перечислен. Там же не сказано,
      как соотносятся `code` и `details`, и как в эту форму ложится
      пользовательский текст, который §20.3 подчиняет §13.5 и §14.6 «наравне с
      экранными» — то есть должен ли сервер отдавать формулировку или только код,
      как это делает `ReasonCode` в FitCore («русские формулировки живут в
      Localization, не здесь»).
- [ ] **3. Тела запросов (§20.3).** У `POST /v1/week-plans` не задано ничего (см.
      также п. 7), у `POST /v1/week-plans/{week_start}/rebuild` — «с причиной»,
      без формы; `RebuildCause` в FitCore имеет семь случаев, часть с
      ассоциированными значениями. У `POST /v1/workouts` не сказано, что именно
      идентифицирует `planned_day`.
- [ ] **4. DTO-слой: ни один тип FitCore не `Codable`.** Ноль вхождений
      `Codable`/`Encodable`/`Decodable` во всём пакете, и `FitCore/Package.swift`
      запрещает зависимости по построению. §20.2 перечисляет пять подкаталогов
      (`Auth`, `DB`, `Mapping`, `Routes`, `Content`), слоя исходящих DTO среди
      них нет, а `Mapping/` описан односторонне — «схема §3.1 → типы FitCore».
      Всё, что §20.3 обещает отдавать, требует второго, не названного слоя:
      `ReasonCode` (enum с ассоциированными значениями и вложенными
      `RebuildCause`, `PatternLimit`, `DayOutcome.Cause`, `SessionKind`,
      `MuscleSlug`, `Phase`), `DayOutcome`, `BuiltSession`/`PrescribedExercise`,
      `CycleState`, `SetOutcome`. Отдельно для `ReasonCode`: §14.6 требует, чтобы
      причина фазового происхождения донесла до клиента `cycleConfidence`, — то
      есть форма обязана его нести, а не терять при уплощении.
- [ ] **5. Вычисление `ETag` для `/v1/content/exercises` и `/stretches`
      (§20.3, §20.11).** «С `ETag` и долгим кешем на клиенте» — от чего он
      считается, не сказано: версия пакета, хеш отданного JSON, отметка сборки.
      §20.11 добавляет ограничение, которое это решение обязано учесть: «правка
      одной опечатки в cue требует деплоя сервера», то есть `ETag` обязан
      меняться от правки контента, а не только от версии сервера.
- [ ] **6. Какие claims проверяются (§20.5, тест 20b).** §20.5 описывает механизм
      (JWKS, асимметрия, кеш, обновление по истечении) и не называет ни одного
      проверяемого claim. При этом 20b требует отклонять «токен другого проекта»
      — значит `iss` и/или `aud` обязаны проверяться, и какой именно, решает не
      реализация. Там же не определены: политика повторного запроса JWKS при
      промахе по `kid`, поведение при недоступности JWKS, и допустимый перекос
      часов для `exp`/`nbf`.

### Входы FitCore и операции без места в схеме

- [x] **7. Конкретные дни недели для `POST /v1/week-plans`** — закрыт
      (`c4c901b`): колонка `profiles.training_weekdays smallint[]`, ISO 1–7,
      уникальность и порядок проверяет `weekdays_canonical`; тело запроса дней
      не принимает; смена дней — новый триггер пересборки §7.1. §7.2 прямым
      текстом: «Вход: `days_per_week`, **конкретные дни недели**,
      `experience_level`», и `Planner.weekGrid(days: [CalendarDay], level:)`
      требует именно даты. В §3.1 `profiles` есть `days_per_week int`, колонки
      под выбранные дни недели нет. Это ровно тот класс дыры, который §20.8
      поймал и закрыл для `userSeed`, — но здесь §20 молчит, и в §19.2 пункта
      тоже нет. Решение определяет схему (§3) — колонка в `profiles`, поле в
      теле запроса или вывод из чего-то ещё.
- [x] **8. `isDeloadWeek`: кто решает и где хранится** — закрыт (`7639770`):
      `week_plans.is_deload`, значение фиксируется при генерации. Правило счёта
      (§19.2 п.4) осталось открытым, и до его закрытия признак всегда `false` —
      записано в §11.5 явно. Тем же коммитом закрыта найденная при прогоне
      ловушка: `Mapping/` обязан отдавать `.noPhases` пользователю без
      `cycle_profiles`, иначе разгрузка не срабатывает молча. Обязательное поле
      `WeekContext` и `SessionInput`. FitCore его намеренно не считает —
      `Readiness.plannedVolumeFactor` и doc `Planner.swift`: «признак передаёт
      вызывающая сторона, правило счёта открыто (SPEC §11.5, §19.2 п.4)».
      Колонки в §3.1 нет, §20 не упоминает вовсе. Два вопроса разной природы:
      правило счёта (§19.2 п.4, открыт продуктово) и место хранения (схема, §3).
      Второе может быть закрыто раньше первого, но не позже миграций.
- [x] **9. Формат `week_plans.input_digest`** — закрыт (`1c23c02`): полный
      снимок входа генерации с номером версии (`{"v": 1, ...}`), назначение —
      отладка и воспроизводимость, а не определение причины пересборки
      (`rebuildNotice` сравнивает выходы и дайджест не читает). Колонка `jsonb not null` с
      комментарием «снимок входных данных на момент генерации, чтобы понимать
      причины пересборки». Ни §3.1, ни §20 не говорят, что в него входит. Сервер
      — единственный, кто его пишет (§20.6), и до первой записи формат должен
      существовать.
- [ ] **10. «Генерация недели» — два вызова с записью между ними, а не один
      `POST`.** `Planner.weekGrid` отдаёт `[GridSlot]` — только (дата, тип), без
      `id` и без вектора; `WeekContext.week` требует уже существующие
      `[PlannedDay]`, а `planRemainingDays` ничего не создаёт. Реальная
      последовательность: `weekGrid` → запись `planned_days` (сервер минтит
      `id`) → `planRemainingDays` → запись состава. §20.3 описывает это одной
      строкой таблицы. Решается вместе с п. 3 и с тем, что происходит при
      повторном `POST` на существующую неделю (`week_plans` имеет
      `unique (user_id, week_start)`).
- [ ] **11. Нет эндпоинта, которым пользователь ставит акцент и меняет тип дня.**
      §7.2: «акцент дню ставит пользователь», «пользователь может в любой день
      заменить тип тренировки. Планировщик принимает это и перебалансирует
      остальное». §20.6 относит `planned_days` к таблицам, которые пишет только
      сервер, а §20.3 не имеет ни REST-`PATCH`, ни действия под это — при том что
      FitCore держит под этот случай `RebuildCause.dayEdited`, у которого сейчас
      не будет ни одного источника. Задевает и правило границы §20.3 («есть
      строка в §3.1 — REST»), поэтому решается в §20.3, а не в обход него.

### Расхождения текста §20 и кода FitCore

- [x] **12. Какой диапазон повторов идёт в `Progression.nextSet`** — закрыт
      вместе с п. 13 (`1bed6c9`, `ab49599`): одна функция `Planner.repRange`
      на предписание, дерево и колонки; 20c дополнен проверкой самого
      диапазона.

      Исходная формулировка пункта, ради причины решения: §20.9 писал предикаты
      через `rep_min`/`rep_max`, не называя, какие именно. В коде это два разных значения: `SessionBuilder`
      кладёт в `workout_exercises` `targetRepMin = table.reps.lowerBound` и
      `targetRepMax = table.reps.upperBound + repExtension`
      (`SessionBuilder.swift:771–772`), а свёртка вызывается с
      `baseRange: goalTable(goal).reps` — без `repExtension`
      (`WeekPlanner.swift:264`). **Тест 20c по построению слеп к этому выбору:**
      он сверяет каждую ветку дерева с «прямым вызовом `Progression.nextSet` на
      том же состоянии», то есть передаёт обеим сторонам один и тот же диапазон.
      Неверный диапазон даст зелёный 20c и неверные веса у пользовательницы.
      Поэтому решение обязано прийти из §9.3/§20.9, а не из реализации, и вместе
      с ним — чем 20c дополняется, чтобы ловить сам выбор.
- [x] **13. Доступ к `Planner.goalTable`** — закрыт вместе с п. 12
      (`1bed6c9`, `ab49599`): публичные `Planner.repRange(goal:state:)` и
      `baseRepRange(goal:)`; вызов — на состоянии, которое использует сборка,
      после сбросов §9.7. Таблица цель → (повторы, RIR) из §9.1
      объявлена `internal` (`SessionBuilder.swift:219`). Сервер не может ни
      получить `baseRange` для п. 12, ни заполнить
      `exercise_states.current_rep_min` и `current_rep_max` (обе `not null`, §3.1)
      — `ExerciseState` их намеренно не несёт («диапазон определяется целью и
      передаётся в функции модуля как `baseRange`»). Дублировать §9.1 на сервере
      значит завести вторую реализацию таблицы, которую §20.1 запрещает по
      смыслу. Решение затрагивает публичный API FitCore, а не только §20.
- [ ] **14. Где живёт логика `/alternatives` и `/substitute`.**
      `ExerciseCandidate.alternatives: [String]` и `familyLoadRatio: Double?` —
      **только поля данных**: их не читает ни одна функция FitCore. §13.4 требует
      три альтернативы, отсортированные по близости профиля вкладов мышц, с
      текстовой причиной пригодности, и перенос веса
      `roundToAchievable(baseline × ratio(новое) / ratio(старое), вниз)` с
      падением в калибровку, когда пересчёт не определён. Ни ранжирования, ни
      переноса в FitCore нет. То есть два из трёх «действий» §20.3 сервер обязан
      реализовать сам — при том что §20.1 запрещает вторую реализацию логики, а
      §20.2 отводит серверу роль «собирает вход FitCore, зовёт его, пишет домен».
      Решение — где эта логика живёт (FitCore, Server, FitContent), а не как её
      написать.
- [x] **15. Часовой пояс: `Timestamp`/`evaluationMoment` против `timestamptz`**
      — закрыт (`76b5d09`): колонки разведены по роли — `muscle_fatigue.updated_at`
      в `timestamp` без зоны, `exercise_states.last_performed_at` в `date`,
      настоящие моменты остаются `timestamptz`; конверсия один раз при записи,
      переход на летнее время — принятая погрешность. Добавлен тест 20i. Контракт FitCore — **местное время, не UTC**:
      `Planner.evaluationMoment` прямым текстом «`Timestamp` и `CalendarDay`
      отсчитываются от одной эпохи **в местном времени**, то есть полночь дня `d`
      — `d.dayNumber × 24` часов» (`WeekPlanner.swift:119`). §20.7 говорит
      только, что сервер живёт в UTC, а клиент присылает локальную
      дату и IANA-зону для валидации. Про то, что `muscle_fatigue.updated_at` и
      `sets.completed_at` (оба `timestamptz`) обязаны переводиться в местную
      шкалу, прежде чем стать `Timestamp`, §20.7 не говорит — а без этого распад
      утомления §8.1 поедет на величину смещения зоны. То же в миниатюре:
      `exercise_states.last_performed_at timestamptz` ↔
      `ExerciseState.lastPerformedAt: CalendarDay?`, где детренированность §9.7
      считается в целых днях и ошибка на сутки меняет ступень. Усложняющее
      обстоятельство — §20.7 **сознательно** отказался хранить зону в `profiles`
      («чтобы не заводить колонку и не ломать поездки в другой пояс»), то есть
      шкала может меняться от запроса к запросу, и решение обязано сказать, что
      происходит с уже записанными значениями при смене зоны.
- [ ] **16. Момент оценки для начатой тренировки.** `SessionInput` требует
      утомление «уже с распадом до момента оценки дня (§7.1: 12:00)». Тренировка,
      начатая в 19:00 через `POST /v1/workouts`, собирается по 12:00 или по
      «сейчас» — §20.3 не говорит, а разница видна в весах.
- [ ] **17. `sets.prescribed_reps int not null` — какое число туда пишется.**
      `PrescribedExercise` несёт `targetRepMin` и `targetRepMax`, числа на
      конкретный подход у него нет. Сервер — единственный, кто пишет `sets`
      (§20.6).
- [ ] **18. Дерево §20.9 для упражнений без веса.** При `ladder == .none`
      (собственный вес, резинки) `roundToAchievable` возвращает значение как
      есть, то есть дерево выдаёт числа для упражнения, у которого веса нет
      вовсе, а `exercise_states.baseline_kg` для него «остаётся nil навсегда».
      §20.9 об упражнениях без веса не говорит; §9.5 для них предписывает рост
      повторами и подходами, а не весом.
- [ ] **19. Предел размера дерева §20.9.** §20.9 требует обрезки по глубине при
      превышении предела узлов, число не выбрано. Уже зафиксировано как открытый
      вопрос (§19.2 п.19) со сроком «замерить на первом срезе контента и
      зафиксировать до этапа В2», то есть закрывается не здесь, но блокирует
      именно этот модуль — и вместе с числом нужно правило, как фронтенд узнаёт,
      что ветка обрезана.
- [ ] **20. Калибровочный ×1.15 и лимит §9.8 — второе место, слепое для 20c.**
      §9.8 требует «не больше 3 повышений за упражнение в калибровке»; `nextSet`
      его не считает (комментарий `SetReaction.swift:50`: «счётчик на уровне
      сессии, которым `Progression` не владеет»), и
      `rebuildStates` тоже («счётчик на уровне сессии, а не между ними»). Дерево
      §20.9 унаследует ровно это поведение, 20c пройдёт зелёным, и расхождение с
      §9.8 останется невидимым. Решение — где живёт счётчик, раз внутри сессии на
      вебе решения принимает дерево, а не сервер.
- [x] **21. Конфликт 20f и 20g в §20.15** — закрыт (`ac9e80d`): 20g переписан
      под вставку (она двигает курсор сама), идемпотентный ретрай курсор не
      двигает благодаря `when (old.* is distinct from new.*)`; триггер на `sets`
      разбит на два, потому что Postgres запрещает `WHEN` по `OLD` в
      INSERT-триггере. 20f не менялся. 20g
      требует «изменён подход уже выгруженной тренировки → `sync_seq`
      родительской строки сдвинут» (тот же сценарий, что §18 37c, но через HTTP),
      а 20f требует отклонять изменение подхода с непустым `completed_at`
      (§4.3, иммутабельность). По HTTP остаются только вставка нового подхода и
      upsert по клиентскому `id` **до** завершения — то есть 20g в формулировке
      «изменён» по этому пути невоспроизводим, а 37c формулировался для пути
      синхронизации FitData, где иммутабельность проверяет другой слой.
      **Прежде чем закрывать любой из двух, нужно установить, что именно 20g
      должен проверять через HTTP** — сдвиг курсора на вставке, на upsert'е
      незавершённого подхода, или 20g в этой формулировке для веба не применим.
      Закрывать их по отдельности нельзя: любое решение по одному меняет смысл
      другого.

### Обязанности Mapping/, не названные в §20

- [ ] **22. `Mapping/` местами обязан считать, а не отображать — и это записано
      только в FitCore.** §20.2 описывает слой как «схема §3.1 → типы FitCore»,
      что верно для большинства срезов (`FatigueState` ← `muscle_fatigue`,
      `CycleEvent` ← `cycle_events`, `SetResult` ← `sets` и так далее — FitCore
      нигде не принимает полную запись, каждый вход уже узкий срез). Но четыре
      места так не работают, все четыре задокументированы в doc-комментариях
      FitCore и ни одно — в §20:
      - `FatigueSet.muscleLoad` — это уже `contribution[m] × fatigue_cost`, и
        doc прямо говорит, что перемножает их вызывающая сторона (данные из
        FitContent);
      - `PainEvent.joint` — `Recovery.primaryJoint(from: jointStress)`, причём doc
        **запрещает** выводить его заново в каждом вызывающем коде («иначе два
        вызывающих кода разошлись бы на ничьих»), а колонки под сустав в §3.1
        нет вовсе (§19.2 п.8, там же сказано, что её нет);
      - `EquipmentProfile` — контракт §7.5 «флаг главнее своей весовой колонки»:
        при `has_kettlebells = false` сервер обязан передать `kettlebellsKg`
        пустым, иначе лестница построится по невидимым гирям;
      - `WeekContext.fatigue` и `SessionInput.fatigue` — **один и тот же словарь
        мышц означает разное**: первый берёт состояние до распада (распад до
        12:00 делает сам `planRemainingDays`), второй — уже с распадом. Положить
        продеканное в `WeekContext` значит применить распад дважды. Рядом:
        `WeekContext.exerciseHistory` обязан быть сырым журналом, а не готовыми
        состояниями («готовое состояние могло быть посчитано с другой лестницей»).

      Решение — где эти контракты фиксируются, чтобы будущий iOS-клиент,
      линкующий тот же `Mapping/` (§20.2 обещает именно это), не написал второе
      соответствие.

### Не решение, а правка текста

- [ ] **23. Три места, где §20 описывает существующий код неверно.** Выбора здесь
      нет, есть правка §20 тем же коммитом, что закрывает соседние пункты:
      - **§20.6 называет `Recovery.applying` «свёрткой».** «`muscle_fatigue`
        сервер пересчитывает из истории подходов через
        `Progression.rebuildStates` и свёртки §8.1» — но doc самой функции прямо
        говорит: «Инкрементально, а не сверткой по всей истории». Пересчёт из
        журнала возможен (функция коммутативна, и это в doc доказано), но это N
        вызовов по одному на момент времени, а не один вызов вроде
        `rebuildStates`. Формулировка обещает симметрию, которой в API нет.
      - **Таблица §20.9 не упоминает потолок в одну достижимую ступень.**
        `nextSet` возвращает `min(stepped, oneStepUp)` вне калибровки
        (`SetReaction.swift:86`, коммит `a185100`), а §20.9 называет только
        «×1.05» и «округляются к лестнице достижимых». Там же не сказано
        направление округления, хотя в
        коде ветки вниз округляются `.down`, вверх — `.up`. На построение дерева
        это не влияет (дерево строится вызовом `nextSet`), но текст расходится с
        кодом и вводит в заблуждение того, кто будет его читать.
      - **`/v1/week-plans/{week_start}/preview` — проекция, а не свой вызов.**
        Заморозка состояния будущего дня (§18 31b) уже реализована внутри
        `planRemainingDays`: готовность считается с `isToday ? todayOverride :
        nil`, утомление — с распадом до `evaluationMoment(day)`. Отдельной
        функции предпросмотра одного дня в FitCore нет, и §20.3 подаёт эндпоинт
        так, будто есть.

## 3. Часть решений §2 определяет схему — миграции пишутся после них, не раньше

`Supabase/migrations/` сейчас содержит только README («Схема ещё не записана»), и
по §17 схема входит в тот же этап В0, что и каркас сервера. Соблазн написать
миграции первыми есть — они выглядят независимыми от §20. Они не независимы.

**Миграции не пишутся, пока не закрыты пункты §2, которые могут добавить или
изменить колонку:**

| Пункт | Что может появиться в схеме |
|---|---|
| 7. Конкретные дни недели | колонка в `profiles` — либо решение, что их там нет и они приходят в теле запроса |
| 8. `isDeloadWeek` | колонка (`week_plans`? `profiles`?) — либо решение, что признак выводится |
| 9. `input_digest` | формата нет; колонка уже объявлена `not null`, то есть первая же вставка обязана знать форму |
| 13. `goalTable` | `exercise_states.current_rep_min`/`current_rep_max` объявлены `not null`, а заполнить их нечем |
| 15. Часовой пояс | §20.7 отказался от колонки зоны сознательно; если решение это пересмотрит, оно меняет `profiles` |
| 21. Конфликт 20f/20g | ответ может затронуть триггеры `sync_seq` из §3.1, которые в миграции и живут |

**Все шесть пунктов таблицы закрыты** (см. §2), и §19.2 п.8 — тоже
(`b93c0dc`): событие несёт множество суставов, колонка `sets.pain_joints`,
факт фиксируется на момент события. Ниже — исходная формулировка, зачем эта
связка вообще существует.

Отдельно, за пределами §20, но в той же миграции: §19.2 п.8 фиксировал, что
**колонки под сустав события боли в §3.1 нет вовсе** — «ни в `sets`, ни отдельной
таблицей». `Mapping/` обязан собирать `PainEvent.joint` (§2 п. 22), и делать это
он может либо пересчётом из FitContent на каждом чтении, либо из колонки,
которой нет. Пункт открыт продуктово (там же предложены три варианта), и до его
закрытия одна из двух дорог просто отсутствует.

Порядок, из этого следующий:

1. ~~Закрыть пункты §2, помеченные выше как схемозначимые (7, 8, 9, 13, 15, 21),
   и решить §19.2 п.8 в части «нужна ли колонка».~~ **Сделано** — ветка
   `spec/server-schema-decisions`, семь коммитов SPEC плюс `ab49599` в FitCore.
2. Написать миграции `0001`–`0005` по порядку из
   `Supabase/migrations/README.md`, включая RLS §3.2.
3. Только потом — интеграционные тесты 20a/20b и всё, что читает БД.

Каркас §20.2 + §20.4 + §20.5 (пакет, пул, JWKS, подстановка claims) от этого
порядка не зависит и может быть написан раньше: он не читает ни одной доменной
таблицы. `Mapping/` и `Routes/` — зависят полностью.

## 4. Где `implement-feature` не подходит для Server как есть

[implement-feature](../implement-feature/SKILL.md) остаётся основой, но для
Server она не работает в четырёх местах, и каждое нужно заменить явно, а не
обойти молча.

1. **§1 п. 1 — «doc-комментарий модуля целиком» как источник архитектуры.** У
   Server его нет: `Server/` не существует и в скелет не входил, тогда как все
   шесть модулей FitCore получили свой на этапе скелета. **Замена:** источник
   архитектуры для Server — сам §20 (в первую очередь §20.2–§20.9), а
   doc-комментарий `Sources/FitServer/FitServer.swift` пишется **первым
   артефактом модуля**, по образцу существующих: конвейер, ключевые типы, что
   модуль обязан закрыть, какие решения приняты и почему. До него кода не
   появляется.
2. **§1 п. 3, §5 и чек-лист — «на каждый сценарий §18 отдельный тест».**
   Обязательные тесты Server — **§20.15, сценарии 20a–20h**, и §20.15 прямо
   говорит: «по образцу §18, но это тесты сервера и фронтенда, а не FitCore».
   Ни один номер §18 за Server не закреплён. **Замена:** правило «по одному
   тесту на сценарий с номером в имени» действует как есть, но по списку
   20a–20h. Исключение — 20c и 20g: см. §2 пп. 12, 20, 21, там тест сам по себе
   не является достаточным.
3. **§2 — граф зависимостей.** Он описывает порядок подмодулей внутри FitCore и
   начинается с «FitCore — zero-dependency: ни SwiftUI, ни SwiftData, ни
   Foundation.Date». Server в этот граф не входит вовсе: он первый пакет с
   внешними зависимостями (Vapor, `postgres-nio`, JWKS-клиент) и первый,
   собираемый под Linux. **Замена:** зависимости Server — `FitCore` и
   `FitContent` (§20.2, §20.11), и проверять перед началом нужно не порядок
   подмодулей, а (а) что срез контента по §20.11 существует, (б) что миграции
   применены (§3), (в) что пакет собирается под Linux. Хорошая новость по (в):
   FitCore к этому готов — во всём пакете четыре строки `import`, и те внутри
   `#if canImport(Darwin) / #elseif canImport(Glibc)` ради `pow`, Foundation не
   импортируется нигде. `FitCore/Package.swift` объявляет
   `platforms: [.iOS(.v17), .macOS(.v14)]`, что задаёт нижнюю границу macOS и
   для Server-пакета.
4. **Чек-лист — «`swift test` пройден в пакете».** Для Server этого
   недостаточно: 20a, 20b, 20e, 20f и 20g требуют **живого Postgres с
   применёнными миграциями и включённым RLS**, двух разных JWT и проверки того,
   что запрос с плохим токеном до БД не доходит. Категории интеграционного теста
   в `implement-feature` нет вовсе, а `Tests/IntegrationTests/` содержит один
   README. **Замена:** «`swift test` чист» остаётся для юнит-части (Mapping,
   построение дерева, `userSeed`), и к нему добавляется отдельный прогон
   интеграционных тестов против локальной базы (`supabase start`, как описывает
   `Supabase/migrations/README.md`). Тест изоляции §20.4 обязателен и не
   заменяется существующим тестом §3.2: тот проверяет PostgREST, а сервер ходит
   другим путём — это сказано в §20.4 прямым текстом.

## 5. Всё остальное — по implement-feature

Ветка от чистого `master`, стиль коммита (императив, английский заголовок, тело с
обоснованием нетривиальных решений), правило «несостыковки фиксировать и
называть пользователю» для всего, что не входит в список §2 — из
[implement-feature](../implement-feature/SKILL.md) без изменений.

§5а (многосессионная симуляция для модулей со свёрткой) к Server не применяется
напрямую: свёртки принадлежат FitCore и уже покрыты в `FitCoreTests`. Но одно
её требование переносится буквально — пересчёт производных после записи подхода
(§20.6) обязан быть проверен не на одном подходе, а на журнале, потому что
именно там 20e требует равенства состояний после ретрая и после одиночной
отправки.

## Чек-лист перед тем, как считать работу в этом домене готовой

- [ ] Все пункты §2 закрыты в SPEC отдельными коммитами до кода; п. 23 внесён как
      правка текста, а не как решение.
- [x] Схемозначимые пункты (7, 8, 9, 13, 15, 21) и §19.2 п. 8 закрыты **до** того,
      как написана первая миграция (§3). Миграций всё ещё нет — порядок соблюдён.
- [ ] `Sources/FitServer/FitServer.swift` содержит doc-комментарий модуля,
      написанный до кода (§4 п. 1).
- [ ] На каждый сценарий 20a–20h — отдельный тест с номером в имени (§4 п. 2).
- [ ] 20c дополнен так, чтобы ловить сам выбор диапазона (§2 п. 12) и лимит
      калибровки (§2 п. 20), а не только совпадение дерева с `nextSet`.
- [ ] 20f и 20g закрыты вместе, после того как установлено, что именно 20g
      проверяет через HTTP (§2 п. 21).
- [ ] Пакет собирается под Linux; интеграционные тесты прогнаны против локальной
      базы с применёнными миграциями и RLS (§4 пп. 3, 4).
- [ ] Тест изоляции §20.4 прогнан через HTTP-эндпоинты с двумя JWT, отдельно —
      подделанная подпись и истёкший токен; существующим тестом §3.2 не заменён.
- [ ] Ни одно решение из §2 не выбрано молча в коде (§1).
- [ ] `git status` чист.
